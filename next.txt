datacenter (storages and servers), data warehouse -> data mart

DB cluster

GPU cluster

benchmark

VM-OS-level (docker)

how nestor manipulates ports and networks in CTIC

Cloud service - IaaS, PaaS, SaaS, XaaS - like MQTT, cluster processing.

git guide

light environment redhat, slackware, archlinux, windows


corregir el navbar menu

modificar las paginas post (añadir una imagen de background)

añadir listas por cada filtro de fecha y categoria en los blogs
